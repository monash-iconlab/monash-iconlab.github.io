// Visitor analytics (Cloudflare Worker). Set enabled: true after deploying the worker.
window.ICONLAB_ANALYTICS = {
  enabled: true,
  endpoint: 'https://iconlab-analytics.monash-iconlab.workers.dev/track'
};
